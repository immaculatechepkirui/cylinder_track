import React, { useEffect } from "react";
// Placeholder component. For the hackathon use Leaflet + onDriverLocations listener.
export default function MapView() {
  useEffect(() => {
    // Initialize Leaflet map here and listen to driver locations
  }, []);
  return <div style={{ height: 300, background: "#eee" }}>Map placeholder (use Leaflet)</div>;
}